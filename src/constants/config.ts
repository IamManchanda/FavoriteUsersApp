export const CONFIG = {
  /**
   * In a real-world application, these values would be stored in a secure location.
   * For the sake of simplicity, we are storing them in a configuration file.
   */
  LOGIN_EMAIL: "reactnative@jetdevs.com",
  LOGIN_PASSWORD: "jetdevs@123",
  RANDOM_USERS_API_URL: "https://randomuser.me/api",
};
